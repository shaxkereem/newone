import { useDisclosure } from "@mantine/hooks";
import {Button} from "@mantine/core";

function TextComponent() {
  return (
    <div>
      <Button variant="filled">This is not button</Button>
    </div>
  );
}

export default TextComponent;
